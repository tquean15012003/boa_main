export class Event {
    // 1. Attribute
    eventId
    eventType

    // 2. Constructor
    constructor(eventId, eventType) {
        this.eventId = eventId
        this.eventType = eventType
    }
}